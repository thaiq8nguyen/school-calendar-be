const router = require('express').Router();

// const authenticate = require('../auth/authenticate-middleware');
const CalSubs = require('./calSubs-model');


router.get('/:cal_id/subscribers/', (req, res) => {

})

router.post('/:cal_id/subscribers/', (req, res) => {

})

router.delete('/:cal_id/subscribers/:id', (req, res) => {

})

router.put('/:cal_id/subscribers/:id', (req, res) => {

})
module.exports = router;